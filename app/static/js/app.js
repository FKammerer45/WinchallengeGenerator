// static/js/app.js - Common JavaScript functions (if any remain)

document.addEventListener("DOMContentLoaded", () => {
    // Log to confirm loading, but might be removed if file becomes empty
    console.log("app.js loaded");

    // --- REDUNDANT challenge form listener removed ---

    // --- UNUSED timer initialization removed ---

    // --- Add any TRULY GLOBAL initializations here if needed ---
    // e.g., initializing Bootstrap tooltips site-wide, if not handled by bundle:
    // var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
    // var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
    //   return new bootstrap.Tooltip(tooltipTriggerEl)
    // })

});

// --- REDUNDANT submitChallengeForm function removed ---

// --- UNUSED initializeTimer function removed ---