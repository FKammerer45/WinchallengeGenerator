// app/static/js/challenge_generator/formConstants.js

export const selectedGames = new Set();
export const PAGE_SIZE = 10; // how many games visible per click

export const formState = {
  isForcingMode: false,
  gamesShown: PAGE_SIZE,
  b2bSegmentCounter: 0,
};
